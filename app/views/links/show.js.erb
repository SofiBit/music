$('#show_div').empty();
$("<%= escape_javascript(render 'show') %>").appendTo("#show_div");

<% if current_user %>
  getStars(<%= @track.id %>, '<%= @track.class %>', <%= current_user.id %>);
<% end %>
