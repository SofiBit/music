<%= form_tag('/tags', method: :post) do %>
  <%= text_field_tag :tags, '', class: "form-control", placeholder: t('playlist.enter_tags') %>
  <%= hidden_field_tag :playlist, @playlist.id %>
  <%= submit_tag t("input.send") %>
<% end %>
