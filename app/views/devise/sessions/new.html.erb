<div class="card form-bg">
<article class="card-body mx-auto" style="max-width: 400px;">
  <h4 class="card-title mt-3 text-center"><%= t('login.h') %></h4>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-group input-group">
      <%= label_tag(:email, "") %><br />
      <%= f.email_field :email, class: "form-control", autofocus: true, autocomplete: "email", placeholder: t('login.email') %>
    </div>

    <div class="form-group input-group">
      <%= label_tag(:password, "") %><br />
      <%= f.password_field :password, class: "form-control", autocomplete: "current-password", placeholder: t('login.password') %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="checkbox mb-3">
        <%= f.check_box :remember_me %>
        <%= f.label  t('login.remember') %>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.submit t('links.log'), class: "btn bg-button btn-block"%>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</article>
</div>
